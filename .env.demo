# Carregado apenas por `npm run demo` (vite --mode demo).
# O build de produção não lê este ficheiro, por isso não há risco de o modo de
# demonstração escapar para o site publicado.
VITE_DEMO_GALLERIES=true
